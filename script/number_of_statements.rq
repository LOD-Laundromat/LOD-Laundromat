prefix def: <https://lodlaundromat.org/def/>
select ?label {
  {
    select (sum(?n) as ?n) {
      ?s def:numberOfQuadruples|def:numberOfTriples ?n .
    }
  }
  bind(replace(?n,"[0-9](?=(?:[0-9]{3})+(?![0-9]))","$0.") as ?label)
}
